<!-- views/partials/_footer.ejs -->
<footer class="site-footer">
  <div class="wrap">
    <small>
      © <%= new Date().getFullYear() %> — Blog Interactif.
      <span class="sep">•</span> Copy right
      <strong><%= locals.authorName || 'Auteur' %></strong>
      <span class="sep">•</span> # Étudiant : <%= locals.studentId || '—' %>
      <span class="sep">•</span> Collège : <%= locals.collegeName || '—' %>
    </small>
  </div>
</footer>
